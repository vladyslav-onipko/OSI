{% from "../templates/reusable.njk" import logoContainer %}
{% set linkClasses = ['fab fa-linkedin-in', 'fab fa-instagram', 'fab fa-twitter', 'fab fa-facebook-f'] %}

{% macro links() %}
  <div class="nav-bar__links">
    {% for class in linkClasses %}
      <a href="#"><i class="{{class}}"></i></a>
    {% endfor %}
  </div>
{% endmacro %}
  
{% macro navigationBar(navItems, only=false) %}
  {% if only %}
    <ul class="nav-bar__items">
      {% for item in navItems %}
        <li class="nav-bar__item"><a href="#">{{item}}</a></li>
      {% endfor %}
    </ul>
  {% else %} 
    {{ logoContainer('Roran') }}
    <ul class="nav-bar__items">
      {% for item in navItems %}
        <li class="nav-bar__item"><a href="#">{{item}}</a></li>
      {% endfor %}
    </ul>
    {{ links() }}
  {% endif %}
{% endmacro %}